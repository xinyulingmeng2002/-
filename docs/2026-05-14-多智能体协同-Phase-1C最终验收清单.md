# 多智能体协同 Phase 1C 最终验收清单

日期：2026-05-14

## 验收结论

当前状态：自动化验收已通过，准真实 CLI 接入验收已通过。

Phase 1C 是否可宣布完成：可以宣布工程侧完成；建议再补一次浏览器人工长跑作为产品侧确认。

Phase 1D 是否可进入：可以进入。

## 验收范围

本清单覆盖 Phase 1C 收束所需的自动化验证：

1. Web 群聊体验。
2. Protocol / Server / Bridge 闭环。
3. Adapter watch / transcript / attentionTags。
4. Typecheck / Build / Diff。
5. 真实接入长跑记录。

## A. Web 群聊体验

命令：

```bash
npm --workspace @ma/web test -- --run apps/web/src/test/room-shell.test.tsx apps/web/src/test/message-composer.test.tsx apps/web/src/test/message-list.test.tsx apps/web/src/test/agent-workspace-page.test.tsx apps/web/src/test/bridge-workspace-client.test.tsx
```

状态：通过。

结果记录：

```text
npm --workspace @ma/web test -- --run apps/web/src/test/room-shell.test.tsx apps/web/src/test/message-composer.test.tsx apps/web/src/test/message-list.test.tsx apps/web/src/test/agent-workspace-page.test.tsx apps/web/src/test/bridge-workspace-client.test.tsx

结果：5 个测试文件通过，22 个测试通过。

备注：首次合并运行时 RoomShell 首个实时订阅测试超过 Vitest 默认 5s；单独运行通过。已将该长用例显式设置为 10s timeout 后，合并验收通过。
```

## B. Protocol / Server / Bridge 闭环

命令：

```bash
npm --workspace @ma/protocol test -- --run packages/protocol/test/protocol.test.ts
npm --workspace @ma/server test -- --run --testTimeout 10000 apps/server/test/messages-api.test.ts apps/server/test/bridge-ingress.test.ts apps/server/test/bridge-egress.test.ts apps/server/test/uploads.test.ts apps/server/test/private-memories-api.test.ts apps/server/test/private-memory-summary-api.test.ts apps/server/test/phase-1b-smoke.test.ts
npm run smoke:phase1b
```

状态：通过。

结果记录：

```text
npm --workspace @ma/protocol test -- --run packages/protocol/test/protocol.test.ts
结果：1 个测试文件通过，5 个测试通过。

npm --workspace @ma/server test -- --run --testTimeout 10000 apps/server/test/messages-api.test.ts apps/server/test/bridge-ingress.test.ts apps/server/test/bridge-egress.test.ts apps/server/test/uploads.test.ts apps/server/test/private-memories-api.test.ts apps/server/test/private-memory-summary-api.test.ts apps/server/test/phase-1b-smoke.test.ts
结果：7 个测试文件通过，38 个测试通过。

npm run smoke:phase1b
结果：1 个测试文件通过，1 个测试通过。

备注：server 多文件集成验收需要显式 10s timeout；默认 5s 在当前环境会让多个首个集成用例误报 timeout。
```

## C. Adapter 能力

命令：

```bash
npm --workspace @ma/bridge-codex test
npm --workspace @ma/bridge-openclaw test
npm --workspace @ma/bridge-generic test
npm --workspace @ma/bridge-shared test
npm run smoke:phase1b:cli
```

状态：通过。

结果记录：

```text
npm --workspace @ma/bridge-codex test
结果：2 个测试文件通过，22 个测试通过。

npm --workspace @ma/bridge-openclaw test
结果：2 个测试文件通过，22 个测试通过。

npm --workspace @ma/bridge-generic test
结果：2 个测试文件通过，16 个测试通过。

npm --workspace @ma/bridge-shared test
结果：2 个测试文件通过，6 个测试通过。

npm run smoke:phase1b:cli
结果：phase1b generic cli smoke passed。

备注：普通沙箱下该命令因不能监听 127.0.0.1 报 EPERM；提升权限后通过。
```

## D. Typecheck / Build / Diff

命令：

```bash
npm run typecheck
npm --workspace @ma/web run build
git diff --check
```

状态：通过。

结果记录：

```text
npm run typecheck
结果：所有 workspace typecheck 通过。

npm --workspace @ma/web run build
结果：Vite production build 通过。

git diff --check
结果：通过。
```

## E. 真实接入长跑记录

目标：

```text
创建邀请钥匙 -> 启动 adapter -> watch transcript -> 人类提及 Agent -> watch 输出 mentioned-you -> 人类回复 Agent -> watch 输出 reply-to-you -> 附件消息 -> 停止/撤销
```

状态：准真实 CLI 接入验收已通过；浏览器人工长跑未在当前终端环境执行。

结果记录：

```text
npm run smoke:phase1b:cli
结果：phase1b generic cli smoke passed。

该脚本启动临时本地 HTTP 服务，通过真实 Generic bridge CLI 验证 invite 文件、session start、message send、workspace snapshot 和 events pull。

当前尚未执行浏览器人工长跑：Web 页面中手工创建 token、启动 watch transcript、人工提及 Agent、人工回复 Agent、上传附件、房主断连/撤销 token。
```

## 当前阻塞

暂无工程阻塞。

产品侧补充建议：进入 Phase 1D 前后，可以再做一次浏览器人工长跑，以确认真实页面操作路径和文档描述一致。

## 进入 Phase 1D 判定

进入条件：

1. A/B/C/D 自动化验证通过，或环境限制被明确记录并有替代验证。
2. E 真实接入长跑完成，或明确记录当前环境阻塞。
3. README 阶段口径准确。
4. 工作树干净，`main` 已推送 `origin/main`。

判定：工程侧满足进入 Phase 1D 条件。

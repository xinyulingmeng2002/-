# 多智能体协作台主线复核补记

- 日期：2026-04-30
- 项目路径：`/mnt/f/ClawWorkspace/09-多智能体协作台`
- 复核来源：
  - `~/.codex/history.jsonl`
  - `~/.codex/sessions/2026/04/11/rollout-2026-04-11T17-21-28-019d7bd8-a267-7853-a1bb-70c71bb54fd0.jsonl`
  - `docs/2026-04-15-多智能体协作台-设计规格.md`
  - `docs/2026-04-15-多智能体协作台-Bridge-Phase-1B-设计规格.md`
  - 当前 `phase-1a-foundation` 实现与 README

## 1. 复核目的

在继续推进下一步实现前，重新核对：

1. 最初设想的主线是否仍然成立
2. 当前实现是否已经发生方向性偏移
3. 如果没有偏移，当前最需要收口的局部问题是什么

## 2. 最初设想的稳定主线

根据原始会话与设计冻结稿，可以确认以下主线没有变化：

1. 这是一个独立项目，只放在小窝工作区附近，不依赖小窝内部结构
2. 平台不能把 OpenClaw、Codex 或其他运行时强绑定进核心
3. 项目目标是长期运行的房间式多 Agent 协作平台
4. 身份边界优先，桥接器只负责传输，不代表发言者本体
5. 原始消息、附件和事件必须先保真，再做摘要、知识提炼与长期沉淀
6. v1 应统一使用“消息 + 附件”模型，而不是把多模态退化成文本 URL

## 3. 当前实现与主线的一致处

当前实现仍然与主线保持一致的部分：

1. `apps/server` 继续作为 canonical state，没有被任一 bridge runtime 反向接管
2. `apps/bridges/codex` 与 `apps/bridges/openclaw` 仍是适配器壳，而不是平台核心
3. 房间、参与者、token、session、ingress、egress、summary snapshot 等骨架已经成形
4. 平台仍保持“先把长期协作底座搭稳，再逐步补强能力层”的阶段策略

结论：

当前没有发生“项目变成 OpenClaw 附庸”或“项目反过来开始重构小窝本体”这类主线级偏移。

## 4. 当前出现的局部分叉

虽然主线未偏，但已经出现两处需要及时收口的局部分叉：

### 4.1 附件语义分叉

Web 人类上传链路已经进入正式附件消息模型：

1. 上传后直接创建 canonical message
2. 时间线按附件卡片渲染

但 bridge 侧附件发送仍然是：

1. `uploadFile()`
2. 再把 `caption + URL` 拼进 `body`
3. 作为普通文本消息发送

这会造成：

1. 人类消息与 Agent 消息在附件语义上不一致
2. 时间线出现两套行为
3. 原始规格中的“统一消息 + 附件模型”被局部打断

### 4.2 文档语义落后

README 仍保留“上传附件后把 URL 作为 canonical message 发进房间”的旧说明。

这与当前 Web 侧已经提交的正式附件消息实现不一致，会误导后续开发和复核。

## 5. 是否发生主线偏移

本轮判断：

1. 没有发生主线级偏移
2. 发生的是“阶段性实现不均衡”和“局部语义分叉”

更准确地说：

1. bridge 骨架推进得比较深
2. 统一附件语义刚开始收口
3. 记忆、沉淀、后台学习仍处于轻量骨架阶段

这属于完成度结构偏斜，不属于目标替换。

## 6. 当前最优先的修正点

在继续新增能力前，当前最应该先收口的是：

1. bridge 附件发送与 Web 附件消息模型统一
2. README 与当前实现重新对齐

推荐的下一步表述应为：

“继续保持独立协作底座路线，但优先收拢附件语义，让人类上传和 bridge 发送都回到统一的正式附件消息模型。”

## 7. 本次复核结论

一句话结论：

主线没有偏，当前该修的不是方向，而是附件语义分叉。
